name: khanpan
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Updated SDK constraint for broader compatibility

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2 # Updated to the latest stable version
  firebase_core: ^2.15.0
  firebase_auth: ^4.7.0
  google_sign_in: ^6.1.0
  sign_in_with_apple: ^4.3.0
#  microsoft_graph: ^0.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0 # Updated to the latest stable version

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/
    - assets/

